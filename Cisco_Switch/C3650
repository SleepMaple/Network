== step 1 ==

show version
show boot

(install mode and bundle mode)
.conf > install mode
.bin > bundle mode

== step 2 ==

if no *.conf:
  software install file "flash:file_name"

else:
  config t
  no boot system
  boot system flash:"file.bin"
  end
  wr

==

